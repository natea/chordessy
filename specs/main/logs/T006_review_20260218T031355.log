=== stdout ===
**APPROVED**

The implementation is correct and meets all T006 requirements:

- **rawgit URL**: Matches the existing `dist/index.html` exactly. The spec explicitly acknowledges this as a known limitation ("URLs still resolve; migrate to jsDelivr in future") — not a bug in this task.
- **Missing `game.css` / `game.js`**: These are expected to be created in subsequent tasks. The HTML correctly references them in anticipation.
- **All 6 required UI sections** are present with appropriate element IDs for JS binding.
- **CDN versions** match the existing arpeggiator page exactly.
- **Script load order** (`shared.js` → `chords-db.js` → `game.js`) respects dependency order.
- **Semantic HTML structure** is clean and well-organized.