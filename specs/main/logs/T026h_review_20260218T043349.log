=== stdout ===
**APPROVED**

The implementation is correct. Here's my review:

**Tests (15/15 passing)** - Well-structured across 5 categories:
- Combo visibility (5): Properly verifies hidden at start, hidden at combo=1, visible at combo>=2, correct count display
- Combo reset (1): Correctly tests that wrong notes reset combo and hide display
- Score multiplication (4): Validates 1x/2x/3x multipliers and 10x cap with exact arithmetic
- Display integration (2): HUD score and feedback popup show multiplied values
- Best combo tracking (2): Tracks highest streak, preserved across resets

**Production code (game.js)** - Clean implementation:
- `onCorrect()` (line 488-531): Increments combo, calculates `comboMultiplier = Math.min(state.combo, 10)`, applies to `(BASE_SCORE + timeBonus)`
- `onIncorrect()` (line 533) and `onTimeout()` (line 573): Both reset `state.combo = 0`
- `updateHUD()` (line 600-615): Shows/hides combo display based on `state.combo > 1` threshold
- `bestCombo` tracked at line 493: `if (state.combo > state.bestCombo) state.bestCombo = state.combo`

**No issues found.** The score formula, multiplier cap, combo visibility threshold, and reset behavior are all consistent between tests and implementation. The test helper `playTargetChord()` correctly simulates real user input via mousedown events on the keyboard.