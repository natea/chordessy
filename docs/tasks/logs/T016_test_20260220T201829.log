=== unit stdout ===

=== unit stderr ===
FAIL tests/enemy-death-particles.test.js
  emitDeathParticles(x, y, tint)
    ✕ method exists on BattleScene (1 ms)
    ✕ generates random count between 20 and 30 (1 ms)
    ✕ creates particle emitter using particle-white texture
    ✕ sets speed range 50-200 (1 ms)
    ✕ sets lifespan to 400ms
    ✕ applies downward gravity
    ✕ scales from 1.0 to 0
    ✕ applies tint parameter to particles
    ✕ emitter starts with emitting false (burst mode) (1 ms)
    ✕ calls explode with particle count
    ✕ auto-destroys emitter after particles expire
  screenShake(duration, intensity)
    ✕ method exists on BattleScene
    ✕ defaults duration to 200ms
    ✕ defaults intensity to 0.005
    ✕ calls cameras.main.shake with duration and intensity
  enemyTint(chord)
    ✕ method exists on BattleScene
    ✕ returns a default color for null/missing chord (1 ms)
    ✕ handles diminished chords
    ✕ handles augmented chords
    ✕ handles minor seventh chords
    ✕ handles dominant seventh chords
    ✕ handles minor chords (1 ms)
  handleEnemyDestroy calls death particles
    ✕ handleEnemyDestroy calls emitDeathParticles
    ✕ handleEnemyDestroy calls enemyTint for particle color
    ✕ handleEnemyDestroy triggers screenShake for lastEnemy
  onCorrectAnswer passes chord data for particles
    ✕ bridge.onCorrectAnswer receives chord property
    ✕ bridge.onCorrectAnswer receives lastEnemy property (1 ms)

  ● emitDeathParticles(x, y, tint) › method exists on BattleScene

    expect(received).toMatch(expected)

    Expected pattern: /emitDeathParticles\s*\(\s*x\s*,\s*y\s*,\s*tint\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      16 | describe('emitDeathParticles(x, y, tint)', () => {
      17 |   test('method exists on BattleScene', () => {
    > 18 |     expect(battleJs).toMatch(/emitDeathParticles\s*\(\s*x\s*,\s*y\s*,\s*tint\s*\)/);
         |                      ^
      19 |   });
      20 |
      21 |   test('generates random count between 20 and 30', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:18:22)

  ● emitDeathParticles(x, y, tint) › generates random count between 20 and 30

    expect(received).toMatch(expected)

    Expected pattern: /20\s*\+\s*Math\.floor\s*\(\s*Math\.random\s*\(\s*\)\s*\*\s*11\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      21 |   test('generates random count between 20 and 30', () => {
      22 |     // 20 + Math.floor(Math.random() * 11) yields 20-30
    > 23 |     expect(battleJs).toMatch(/20\s*\+\s*Math\.floor\s*\(\s*Math\.random\s*\(\s*\)\s*\*\s*11\s*\)/);
         |                      ^
      24 |   });
      25 |
      26 |   test('creates particle emitter using particle-white texture', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:23:22)

  ● emitDeathParticles(x, y, tint) › creates particle emitter using particle-white texture

    expect(received).toMatch(expected)

    Expected pattern: /this\.add\.particles\s*\(\s*x\s*,\s*y\s*,\s*['"]particle-white['"]/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      25 |
      26 |   test('creates particle emitter using particle-white texture', () => {
    > 27 |     expect(battleJs).toMatch(/this\.add\.particles\s*\(\s*x\s*,\s*y\s*,\s*['"]particle-white['"]/);
         |                      ^
      28 |   });
      29 |
      30 |   test('sets speed range 50-200', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:27:22)

  ● emitDeathParticles(x, y, tint) › sets speed range 50-200

    expect(received).toMatch(expected)

    Expected pattern: /speed\s*:\s*\{\s*min\s*:\s*50\s*,\s*max\s*:\s*200\s*\}/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      29 |
      30 |   test('sets speed range 50-200', () => {
    > 31 |     expect(battleJs).toMatch(/speed\s*:\s*\{\s*min\s*:\s*50\s*,\s*max\s*:\s*200\s*\}/);
         |                      ^
      32 |   });
      33 |
      34 |   test('sets lifespan to 400ms', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:31:22)

  ● emitDeathParticles(x, y, tint) › sets lifespan to 400ms

    expect(received).toMatch(expected)

    Expected pattern: /lifespan\s*:\s*400/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      33 |
      34 |   test('sets lifespan to 400ms', () => {
    > 35 |     expect(battleJs).toMatch(/lifespan\s*:\s*400/);
         |                      ^
      36 |   });
      37 |
      38 |   test('applies downward gravity', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:35:22)

  ● emitDeathParticles(x, y, tint) › applies downward gravity

    expect(received).toMatch(expected)

    Expected pattern: /gravityY\s*:\s*80/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      37 |
      38 |   test('applies downward gravity', () => {
    > 39 |     expect(battleJs).toMatch(/gravityY\s*:\s*80/);
         |                      ^
      40 |   });
      41 |
      42 |   test('scales from 1.0 to 0', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:39:22)

  ● emitDeathParticles(x, y, tint) › scales from 1.0 to 0

    expect(received).toMatch(expected)

    Expected pattern: /scale\s*:\s*\{\s*start\s*:\s*1\.0\s*,\s*end\s*:\s*0\s*\}/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      41 |
      42 |   test('scales from 1.0 to 0', () => {
    > 43 |     expect(battleJs).toMatch(/scale\s*:\s*\{\s*start\s*:\s*1\.0\s*,\s*end\s*:\s*0\s*\}/);
         |                      ^
      44 |   });
      45 |
      46 |   test('applies tint parameter to particles', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:43:22)

  ● emitDeathParticles(x, y, tint) › applies tint parameter to particles

    expect(received).toMatch(expected)

    Expected pattern: /tint\s*:\s*tint/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      45 |
      46 |   test('applies tint parameter to particles', () => {
    > 47 |     expect(battleJs).toMatch(/tint\s*:\s*tint/);
         |                      ^
      48 |   });
      49 |
      50 |   test('emitter starts with emitting false (burst mode)', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:47:22)

  ● emitDeathParticles(x, y, tint) › emitter starts with emitting false (burst mode)

    expect(received).toMatch(expected)

    Expected pattern: /emitting\s*:\s*false/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      49 |
      50 |   test('emitter starts with emitting false (burst mode)', () => {
    > 51 |     expect(battleJs).toMatch(/emitting\s*:\s*false/);
         |                      ^
      52 |   });
      53 |
      54 |   test('calls explode with particle count', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:51:22)

  ● emitDeathParticles(x, y, tint) › calls explode with particle count

    expect(received).toMatch(expected)

    Expected pattern: /emitter\.explode\s*\(\s*count\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      53 |
      54 |   test('calls explode with particle count', () => {
    > 55 |     expect(battleJs).toMatch(/emitter\.explode\s*\(\s*count\s*\)/);
         |                      ^
      56 |   });
      57 |
      58 |   test('auto-destroys emitter after particles expire', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:55:22)

  ● emitDeathParticles(x, y, tint) › auto-destroys emitter after particles expire

    expect(received).toMatch(expected)

    Expected pattern: /this\.time\.delayedCall\s*\(\s*500/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      57 |
      58 |   test('auto-destroys emitter after particles expire', () => {
    > 59 |     expect(battleJs).toMatch(/this\.time\.delayedCall\s*\(\s*500/);
         |                      ^
      60 |     expect(battleJs).toMatch(/emitter\.destroy\s*\(\s*\)/);
      61 |   });
      62 | });

      at Object.toMatch (tests/enemy-death-particles.test.js:59:22)

  ● screenShake(duration, intensity) › method exists on BattleScene

    expect(received).toMatch(expected)

    Expected pattern: /screenShake\s*\(\s*duration\s*,\s*intensity\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      67 | describe('screenShake(duration, intensity)', () => {
      68 |   test('method exists on BattleScene', () => {
    > 69 |     expect(battleJs).toMatch(/screenShake\s*\(\s*duration\s*,\s*intensity\s*\)/);
         |                      ^
      70 |   });
      71 |
      72 |   test('defaults duration to 200ms', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:69:22)

  ● screenShake(duration, intensity) › defaults duration to 200ms

    expect(received).toMatch(expected)

    Expected pattern: /duration\s*=\s*duration\s*\|\|\s*200/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      71 |
      72 |   test('defaults duration to 200ms', () => {
    > 73 |     expect(battleJs).toMatch(/duration\s*=\s*duration\s*\|\|\s*200/);
         |                      ^
      74 |   });
      75 |
      76 |   test('defaults intensity to 0.005', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:73:22)

  ● screenShake(duration, intensity) › defaults intensity to 0.005

    expect(received).toMatch(expected)

    Expected pattern: /intensity\s*=\s*intensity\s*\|\|\s*0\.005/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      75 |
      76 |   test('defaults intensity to 0.005', () => {
    > 77 |     expect(battleJs).toMatch(/intensity\s*=\s*intensity\s*\|\|\s*0\.005/);
         |                      ^
      78 |   });
      79 |
      80 |   test('calls cameras.main.shake with duration and intensity', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:77:22)

  ● screenShake(duration, intensity) › calls cameras.main.shake with duration and intensity

    expect(received).toMatch(expected)

    Expected pattern: /this\.cameras\.main\.shake\s*\(\s*duration\s*,\s*intensity\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      79 |
      80 |   test('calls cameras.main.shake with duration and intensity', () => {
    > 81 |     expect(battleJs).toMatch(/this\.cameras\.main\.shake\s*\(\s*duration\s*,\s*intensity\s*\)/);
         |                      ^
      82 |   });
      83 | });
      84 |

      at Object.toMatch (tests/enemy-death-particles.test.js:81:22)

  ● enemyTint(chord) › method exists on BattleScene

    expect(received).toMatch(expected)

    Expected pattern: /enemyTint\s*\(\s*chord\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      88 | describe('enemyTint(chord)', () => {
      89 |   test('method exists on BattleScene', () => {
    > 90 |     expect(battleJs).toMatch(/enemyTint\s*\(\s*chord\s*\)/);
         |                      ^
      91 |   });
      92 |
      93 |   test('returns a default color for null/missing chord', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:90:22)

  ● enemyTint(chord) › returns a default color for null/missing chord

    expect(received).toMatch(expected)

    Expected pattern: /if\s*\(\s*!chord\s*\|\|\s*!chord\.symbol\s*\)\s*return\s+0xff4444/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      92 |
      93 |   test('returns a default color for null/missing chord', () => {
    > 94 |     expect(battleJs).toMatch(/if\s*\(\s*!chord\s*\|\|\s*!chord\.symbol\s*\)\s*return\s+0xff4444/);
         |                      ^
      95 |   });
      96 |
      97 |   test('handles diminished chords', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:94:22)

  ● enemyTint(chord) › handles diminished chords

    expect(received).toMatch(expected)

    Expected pattern: /includes\s*\(\s*['"]dim['"]\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

       96 |
       97 |   test('handles diminished chords', () => {
    >  98 |     expect(battleJs).toMatch(/includes\s*\(\s*['"]dim['"]\s*\)/);
          |                      ^
       99 |   });
      100 |
      101 |   test('handles augmented chords', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:98:22)

  ● enemyTint(chord) › handles augmented chords

    expect(received).toMatch(expected)

    Expected pattern: /includes\s*\(\s*['"]aug['"]\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      100 |
      101 |   test('handles augmented chords', () => {
    > 102 |     expect(battleJs).toMatch(/includes\s*\(\s*['"]aug['"]\s*\)/);
          |                      ^
      103 |   });
      104 |
      105 |   test('handles minor seventh chords', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:102:22)

  ● enemyTint(chord) › handles minor seventh chords

    expect(received).toMatch(expected)

    Expected pattern: /includes\s*\(\s*['"]m7['"]\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      104 |
      105 |   test('handles minor seventh chords', () => {
    > 106 |     expect(battleJs).toMatch(/includes\s*\(\s*['"]m7['"]\s*\)/);
          |                      ^
      107 |   });
      108 |
      109 |   test('handles dominant seventh chords', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:106:22)

  ● enemyTint(chord) › handles dominant seventh chords

    expect(received).toMatch(expected)

    Expected pattern: /includes\s*\(\s*['"]7['"]\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      108 |
      109 |   test('handles dominant seventh chords', () => {
    > 110 |     expect(battleJs).toMatch(/includes\s*\(\s*['"]7['"]\s*\)/);
          |                      ^
      111 |   });
      112 |
      113 |   test('handles minor chords', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:110:22)

  ● enemyTint(chord) › handles minor chords

    expect(received).toMatch(expected)

    Expected pattern: /includes\s*\(\s*['"]m['"]\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      113 |   test('handles minor chords', () => {
      114 |     // Checks for 'm' or 'min'
    > 115 |     expect(battleJs).toMatch(/includes\s*\(\s*['"]m['"]\s*\)/);
          |                      ^
      116 |   });
      117 | });
      118 |

      at Object.toMatch (tests/enemy-death-particles.test.js:115:22)

  ● handleEnemyDestroy calls death particles › handleEnemyDestroy calls emitDeathParticles

    expect(received).toMatch(expected)

    Expected pattern: /handleEnemyDestroy[\s\S]*?this\.emitDeathParticles\s*\(/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      122 | describe('handleEnemyDestroy calls death particles', () => {
      123 |   test('handleEnemyDestroy calls emitDeathParticles', () => {
    > 124 |     expect(battleJs).toMatch(/handleEnemyDestroy[\s\S]*?this\.emitDeathParticles\s*\(/);
          |                      ^
      125 |   });
      126 |
      127 |   test('handleEnemyDestroy calls enemyTint for particle color', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:124:22)

  ● handleEnemyDestroy calls death particles › handleEnemyDestroy calls enemyTint for particle color

    expect(received).toMatch(expected)

    Expected pattern: /handleEnemyDestroy[\s\S]*?this\.enemyTint\s*\(/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      126 |
      127 |   test('handleEnemyDestroy calls enemyTint for particle color', () => {
    > 128 |     expect(battleJs).toMatch(/handleEnemyDestroy[\s\S]*?this\.enemyTint\s*\(/);
          |                      ^
      129 |   });
      130 |
      131 |   test('handleEnemyDestroy triggers screenShake for lastEnemy', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:128:22)

  ● handleEnemyDestroy calls death particles › handleEnemyDestroy triggers screenShake for lastEnemy

    expect(received).toMatch(expected)

    Expected pattern: /handleEnemyDestroy[\s\S]*?data\.lastEnemy[\s\S]*?this\.screenShake\s*\(\s*200\s*,\s*0\.005\s*\)/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      130 |
      131 |   test('handleEnemyDestroy triggers screenShake for lastEnemy', () => {
    > 132 |     expect(battleJs).toMatch(/handleEnemyDestroy[\s\S]*?data\.lastEnemy[\s\S]*?this\.screenShake\s*\(\s*200\s*,\s*0\.005\s*\)/);
          |                      ^
      133 |   });
      134 | });
      135 |

      at Object.toMatch (tests/enemy-death-particles.test.js:132:22)

  ● onCorrectAnswer passes chord data for particles › bridge.onCorrectAnswer receives chord property

    expect(received).toMatch(expected)

    Expected pattern: /bridge\.onCorrectAnswer\s*\(\s*\{[^}]*chord\s*:/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      139 | describe('onCorrectAnswer passes chord data for particles', () => {
      140 |   test('bridge.onCorrectAnswer receives chord property', () => {
    > 141 |     expect(battleJs).toMatch(/bridge\.onCorrectAnswer\s*\(\s*\{[^}]*chord\s*:/);
          |                      ^
      142 |   });
      143 |
      144 |   test('bridge.onCorrectAnswer receives lastEnemy property', () => {

      at Object.toMatch (tests/enemy-death-particles.test.js:141:22)

  ● onCorrectAnswer passes chord data for particles › bridge.onCorrectAnswer receives lastEnemy property

    expect(received).toMatch(expected)

    Expected pattern: /bridge\.onCorrectAnswer\s*\(\s*\{[^}]*lastEnemy/
    Received string:  "window.Chordessy = window.Chordessy || {};·
    (function(C) {
      'use strict';·
      class BattleBridge {
        constructor({ scene, keyboardContainer, keyElements }) {
          this.scene = scene;
          this.keyboardContainer = keyboardContainer;
          this.keyElements = keyElements;
          this.keyXMap = new Map();
          this.heldNotes = new Set();
          this.targetMidi = [];
          this.emitter = new Phaser.Events.EventEmitter();
        }·
        buildKeyXMap() {
          this.keyXMap.clear();·
          if (!this.scene || !this.scene.game || !this.keyElements) return;·
          let canvasRect = this.scene.game.canvas.getBoundingClientRect();·
          this.keyElements.forEach(keyEl => {
            let keyRect = keyEl.getBoundingClientRect();
            let centerX = keyRect.left + keyRect.width / 2 - canvasRect.left;
            let midiNote = parseInt(keyEl.dataset.note, 10);
            let isAccidental = keyEl.classList.contains('accidental');·
            if (!isNaN(midiNote)) {
              this.keyXMap.set(midiNote, {
                x: centerX,
                width: keyRect.width,
                isAccidental: isAccidental
              });
            }
          });·
          return this.keyXMap;
        }·
        rebuildKeyXMap() {
          return this.buildKeyXMap();
        }·
        noteOn(midiNote) {
          if (this.heldNotes.has(midiNote)) return;·
          this.heldNotes.add(midiNote);·
          let keyX = this.keyXMap.get(midiNote);
          let x = keyX ? keyX.x : 0;·
          let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
          let isCorrect = targetPitchClasses.has(midiNote % 12);·
          this.emitter.emit('noteOn', { midiNote, x, isCorrect });·
          if (this.audio) {
            this.audio.playNote(midiNote, 0.7);
          }·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.add('pressed');
            }
          }·
          this.checkChord();
        }·
        noteOff(midiNote) {
          if (!this.heldNotes.has(midiNote)) return;·
          this.heldNotes.delete(midiNote);·
          this.emitter.emit('noteOff', { midiNote });·
          if (this.keyElements) {
            let idx = midiNote - C.MIN_NOTE;
            if (idx >= 0 && idx < this.keyElements.length) {
              this.keyElements[idx].classList.remove('pressed');
            }
          }
        }·
        setTargetChord(chordSymbol, midiNotes) {
          this.targetSymbol = chordSymbol;
          this.targetMidi = midiNotes || [];·
          this.emitter.emit('newTarget', { symbol: chordSymbol, midiNotes: this.targetMidi });
        }·
        checkChord() {
          if (this.targetMidi.length === 0) return;·
          let heldArray = Array.from(this.heldNotes);
          if (heldArray.length === 0) return;·
          if (window.Chordessy.pitchClassesMatch(heldArray, this.targetMidi)) {
            this.emitter.emit('chordComplete', { heldNotes: heldArray });
          } else {
            let targetPitchClasses = new Set(this.targetMidi.map(n => n % 12));
            let wrongNotes = heldArray.filter(n => !targetPitchClasses.has(n % 12));·
            wrongNotes.forEach(wrongNote => {
              this.emitter.emit('wrongNote', { midiNote: wrongNote });
            });
          }
        }·
        onBulletHit() {
          this.emitter.emit('bulletHit');
        }·
        onWaveCleared() {
          this.emitter.emit('waveCleared');
        }·
        onGameOver() {
          this.emitter.emit('gameOver');
        }·
        init({ audio, qwertyMap }) {
          this.audio = audio;
          this.qwertyMap = qwertyMap;·
          this.setupMidiInput();
          this.setupQwertyInput();
          this.setupMouseTouchInput();
        }·
        setupMidiInput() {
          C.setupMidi()
            .then(midi => {
              midi.onNoteOn((note, velocity) => {
                this.noteOn(note);
              });
              midi.onNoteOff(note => {
                this.noteOff(note);
              });
            })
            .catch(err => {
              console.warn('MIDI not available:', err);
            });
        }·
        setupQwertyInput() {
          document.addEventListener('keydown', e => {
            if (e.repeat) return;
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              e.preventDefault();
              this.noteOn(note);
            }
          });·
          document.addEventListener('keyup', e => {
            let note = this.qwertyMap[e.key.toLowerCase()];
            if (note !== undefined) {
              this.noteOff(note);
            }
          });
        }·
        setupMouseTouchInput() {
          let touchedNotes = new Set();·
          let handleTouch = e => {
            e.preventDefault();
            let currentTouches = new Set();
            for (let touch of Array.from(e.touches)) {
              let el = document.elementFromPoint(touch.clientX, touch.clientY);
              if (el && el.classList && el.classList.contains('key')) {
                let note = parseInt(el.dataset.note, 10);
                if (!isNaN(note)) {
                  currentTouches.add(note);
                }
              }
            }·
            currentTouches.forEach(note => {
              if (!touchedNotes.has(note)) {
                this.noteOn(note);
                touchedNotes.add(note);
              }
            });·
            touchedNotes.forEach(note => {
              if (!currentTouches.has(note)) {
                this.noteOff(note);
                touchedNotes.delete(note);
              }
            });
          };·
          this.keyElements.forEach(keyEl => {
            let note = parseInt(keyEl.dataset.note, 10);
            if (isNaN(note)) return;·
            let mouseDown = false;·
            keyEl.addEventListener('mousedown', e => {
              e.preventDefault();
              mouseDown = true;
              this.noteOn(note);
            });·
            keyEl.addEventListener('mouseenter', () => {
              if (mouseDown) {
                this.noteOn(note);
              }
            });·
            keyEl.addEventListener('mouseup', () => {
              mouseDown = false;
              this.noteOff(note);
            });·
            keyEl.addEventListener('mouseleave', () => {
              if (mouseDown) {
                mouseDown = false;
                this.noteOff(note);
              }
            });
          });·
          document.addEventListener('mouseup', () => {
            touchedNotes.forEach(note => {
              this.noteOff(note);
              touchedNotes.delete(note);
            });
          });·
          this.keyboardContainer.addEventListener('touchstart', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchmove', handleTouch, { passive: false });
          this.keyboardContainer.addEventListener('touchend', e => {
            e.preventDefault();
            handleTouch(e);
          });
        }
      }·
      C.BattleBridge = BattleBridge;·
      class Enemy extends Phaser.GameObjects.Container {
        constructor(scene, midiNote, x, y, isAccidental) {
          super(scene, x, y);·
          this.midiNote = midiNote;
          this.isAccidental = isAccidental;
          this.alive = true;·
          const glowRadius = isAccidental ? 24 : 30;
          const bodyRadius = isAccidental ? 16 : 20;
          const bodyColor = isAccidental ? 0xff00ff : 0x00ffff;·
          this.glow = scene.add.ellipse(0, 0, glowRadius * 2, glowRadius * 2, bodyColor);
          this.glow.setAlpha(0.3);·
          this.body = scene.add.ellipse(0, 0, bodyRadius * 2, bodyRadius * 2, bodyColor);·
          this.label = scene.add.text(0, 0, '', {
            fontSize: '14px',
            color: '#ffffff',
            fontStyle: 'bold'
          });
          this.label.setOrigin(0.5, 0.5);
          this.label.setVisible(false);·
          this.add([this.glow, this.body, this.label]);·
          scene.add.existing(this);
        }·
        spawnAnimation() {
          const startY = this.y;
          const offScreenY = -100;·······
          this.y = offScreenY;
          this.setAlpha(0);·······
          this.scene.tweens.add({
            targets: this,
            alpha: 1,
            y: startY,
            duration: 500,
            ease: Phaser.Math.Easing.Back.Out
          });
        }·
        die() {
          this.alive = false;·
          if (this.scene.emitDeathParticles) {
            let tint = this.body.fillColor;
            this.scene.emitDeathParticles(this.x, this.y, tint);
          }·
          this.scene.tweens.add({
            targets: this,
            alpha: 0,
            scaleX: 0,
            scaleY: 0,
            duration: 400,
            ease: Phaser.Math.Easing.Back.In,
            onComplete: () => {
              this.destroy();
            }
          });
        }·
        showLabel() {
          let level = this.scene.gameState ? this.scene.gameState.level : 1;
          if (level >= 15) {
            let noteName = C.NOTE_NAMES[this.midiNote % 12];
            this.label.setText(noteName);
            this.label.setVisible(true);
          }
        }
      }·
      C.Enemy = Enemy;·
    })(window.Chordessy);"

      143 |
      144 |   test('bridge.onCorrectAnswer receives lastEnemy property', () => {
    > 145 |     expect(battleJs).toMatch(/bridge\.onCorrectAnswer\s*\(\s*\{[^}]*lastEnemy/);
          |                      ^
      146 |   });
      147 | });
      148 |

      at Object.toMatch (tests/enemy-death-particles.test.js:145:22)

Test Suites: 1 failed, 1 total
Tests:       27 failed, 27 total
Snapshots:   0 total
Time:        0.283 s, estimated 1 s
Ran all test suites matching /tests\/enemy-death-particles/i.
