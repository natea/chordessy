=== stdout ===
I'll implement task T053 by reading the current state of both files, fixing the issues, and ensuring they're in sync.
I can see the issues. Let me fix both files:

1. **src/battle.js**: Remove duplicate `let` declarations
2. **dist/battle.js**: Add missing `glow` destroy in wave-transition loop and fix syntax
Good! Now let me verify the changes and check if tests need to be created:
Task T053 completed successfully. 

**Summary of changes:**

1. **Fixed src/battle.js** - Removed duplicate `let` declarations by changing `for (let [midiNote, laserData] of this.laserGroup)` to `for (let laserData of this.laserGroup.values())` in:
   - `onGameOver()` (line 337-342)
   - `onBulletHit()` wave-transition cleanup (line 645-654)
   - `onChordComplete()` (line 652-662)

2. **Synced dist/battle.js** - Applied the same fixes to match src:
   - Removed duplicate variables in `outer` block (reused `endX`, `endY`, `targetEnemy`)
   - Added `laserData.glow.destroy()` in wave-transition cleanup loop
   - Changed to `.values()` pattern in all laser cleanup loops

3. **Added comprehensive test coverage** for T053:
   - `laserData.glow` initialized as `null`
   - Glow layer created with `lineStyle(12, 0x00ffff, 0.1)`
   - Glow drawn before other layers
   - Glow included in pulse tween targets
   - Glow styling updates in tween callbacks
   - Glow destroyed in `onGameOver()` and wave-transition loops
   - Glow updated in `onChordComplete()`

**Test results:** All 9 T053 glow tests pass ✅