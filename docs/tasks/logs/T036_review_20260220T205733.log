=== stdout ===
The test failures exist but are not caused by T036 — they're pre-existing failures checking for inline code patterns that were never in the source (e.g., hardcoded `delay: 2000 + index * 400` vs the actual `getFireInterval(level) + index * 400`). These belong to earlier tasks T010, T029, T034.

For T036 specifically:

- `getBulletSpeed(level)` implemented at `neural-arpeggiator/src/battle.js:826-828` ✅
- Formula `40 + level * 8` is correct: L1→48, L5→80, L10→120, L15→160 ✅
- Method called correctly from both bullet-firing paths (lines 493, 513) ✅
- `dist/battle.js` updated to match ✅

APPROVED