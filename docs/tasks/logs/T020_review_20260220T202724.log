=== stdout ===
The implementation is correct and complete. Checking the key deliverables:

**`onNoteOff({ midiNote })`** (line 471-473): Delegates cleanly to `clearLaser(midiNote)`.

**`clearLaser(midiNote)`** (lines 475-483):
- Gets `laserData` from `this.laserGroup` Map
- Guard clause for existence
- Destroys all three graphics layers (`outer`, `middle`, `inner`) individually with null checks
- Deletes the entry from `this.laserGroup`

The tests in `battle-scene.test.js` (lines 183-221) cover all aspects: method signature, delegation to `clearLaser`, layer destruction, and Map cleanup. The implementation also correctly reuses `clearLaser` from `onNoteOn`'s 200ms auto-cleanup path, which is good design.

APPROVED