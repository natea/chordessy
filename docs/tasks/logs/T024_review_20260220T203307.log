=== stdout ===
The implementation checks out against all three requirements:

1. **Timing** — `spawnEnemies` at line 385-390: fires after `lastEnemySpawnDelay + 1000ms`, meaning 1000ms after the last enemy spawn animation starts (the "ready beat"), and only for `level >= 1 && level <= 9`.

2. **Random alive enemy** — `getRandomAliveEnemy()` at line 393-397: filters `this.enemies` by `e.alive`, then uses `Phaser.Utils.Array.GetRandom`.

3. **Speed formula** — `spawnBullet(level)` at line 399-410: `baseSpeed = 40`, `speedIncrement = 8`, `speed = baseSpeed + level * speedIncrement`. Matches the spec exactly.

The dist file mirrors the src. No T024-specific tests were added to the test suite (tests pass based on existing Bullet/BattleScene coverage).

APPROVED