=== stdout ===
REJECTED

The test `has particle trail emitter with short lifespan` is **failing**:

```
✕ has particle trail emitter with short lifespan (1 ms)
```

**Root cause:** `battle-scene.test.js:247` checks for `lifespan: 300`, but the implementation at `src/battle.js:1159` uses `lifespan: 200`.

The task spec requires **200ms lifespan**, so the implementation is correct — but the test was written with `300` instead of `200`. The test needs to be corrected to match the spec:

```js
// battle-scene.test.js:247 - should be:
expect(battleJs).toMatch(/lifespan:\s*200/);
```

Additionally, there's a minor redundancy in the implementation: the emitter is configured with `frequency: 16` (auto-emit) but `update()` also calls `emitParticleAt()` manually with `Phaser.Math.Between(5, 8)`. The auto-emitter will fire independently of the manual calls. This doesn't break anything given the emitter starts with `on: false`, but the `frequency: 16` config is unused dead config.