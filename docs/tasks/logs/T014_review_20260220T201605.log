=== stdout ===
The implementation in `src/battle.js` and `dist/battle.js` matches the task requirements:

- `class Enemy extends Phaser.GameObjects.Container` ✓
- Constructor signature `(scene, midiNote, x, y, isAccidental)` ✓
- Glow ellipse with `alpha 0.3` ✓
- White-key: body radius ~20px cyan (`0x00ffff`) ✓
- Black-key (accidental): body radius ~16px purple (`0xff00ff`) ✓
- Label text hidden by default (`setVisible(false)`) ✓
- All three children added to the container via `this.add([this.glow, this.body, this.label])` ✓

APPROVED