=== stdout ===
REJECTED

Two issues with this T010 implementation:

**1. `dist/battle.js` is missing the T010 methods**

`dist/battle.js:98-108` has `BattleBridge` with only T009's constructor — no `buildKeyXMap()` or `rebuildKeyXMap()`. The implementation exists in the new `src/battle.js`, but that file is a separate standalone module not reflected in dist.

```js
// dist/battle.js lines 97-108 — comment even says T009
// --- Battle Bridge (T009) ---
class BattleBridge {
  constructor({ scene, keyboardContainer, keyElements }) {
    ...
  }
  // buildKeyXMap() and rebuildKeyXMap() are ABSENT
}
```

**2. No T010-specific tests**

`tests/battle-bridge.test.js` only verifies T009 constructor properties. There are no assertions that `buildKeyXMap()` or `rebuildKeyXMap()` exist, return the right shape, or correctly compute canvas-relative coordinates. Tests pass because no T010 contract was actually tested.

Required fixes:
- Add `buildKeyXMap()` and `rebuildKeyXMap()` to `BattleBridge` in `dist/battle.js`
- Add tests verifying: methods exist, `keyXMap` is populated with `{x, width, isAccidental}` per MIDI note, and `x` is canvas-relative (subtract canvas left from key center)