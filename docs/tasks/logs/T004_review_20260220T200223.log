=== stdout ===
The implementation satisfies all requirements for T004:

- **Keyboard via shared.js**: `init()` calls `C.buildKeyboard(keyboardContainer)` using `document.getElementById('keyboard-area')` — delegates to `shared.js`'s exported `buildKeyboard`
- **Audio setup**: Creates `AudioContext` (`window.AudioContext || window.webkitAudioContext`)
- **MIDI setup**: Calls `navigator.requestMIDIAccess()` with graceful fallback on error
- **Phaser game instantiation**: `new Phaser.Game(config)` with:
  - `parent: 'phaser-container'` ✓
  - `type: Phaser.AUTO` ✓
  - `transparent: true` ✓
- **`phaser-container` div** present in `battle.html` ✓
- **`shared.js`** exports `buildKeyboard` and is the correct companion file ✓

APPROVED