=== stdout ===
Both the source and dist files are aligned. The implementation is correct and complete:

**`Enemy.die()`** (`src/battle.js:393`):
- Sets `this.alive = false`
- Calls `scene.emitDeathParticles()` if available, passing position and fill color as tint
- Tweens to alpha 0 + scale 0 over 400ms, then destroys on complete

**`Enemy.showLabel()`** (`src/battle.js:414`):
- Reads level from `scene.gameState`, defaults to 1
- Only shows label for level >= 15
- Sets text to note name via `C.NOTE_NAMES[midiNote % 12]`

**Supporting infrastructure** in `dist/battle.js`:
- `emitDeathParticles(x, y, tint)` — burst of 20–30 particles, `lifespan: 400`, `gravityY: 80`, `scale: {start: 1.0, end: 0}`, auto-destroys emitter after 500ms
- `screenShake(duration, intensity)` — defaults 200ms / 0.005
- `enemyTint(chord)` — maps chord type to color (dim, aug, m7, 7, m)
- `handleEnemyDestroy` properly wires particles + screen shake

Tests in `tests/enemy-death-particles.test.js` validate all spec requirements against the dist file, and they passed.

APPROVED