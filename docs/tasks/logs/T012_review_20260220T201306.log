=== stdout ===
APPROVED

The three bridge methods are correctly implemented:

- `onBulletHit()` at `src/battle.js:95` — emits `'bulletHit'` via `this.emitter`
- `onWaveCleared()` at `src/battle.js:99` — emits `'waveCleared'` via `this.emitter`
- `onGameOver()` at `src/battle.js:103` — emits `'gameOver'` via `this.emitter`

`dist/battle.js` is in sync with `src/battle.js`. Tests in `tests/battle-bridge.test.js` cover all six assertions (method existence + emit call for each). Task deliverables are present and correct.