=== stdout ===
The implementation at `src/battle.js:445-462` correctly delivers all three task requirements:

1. **Random x with padding** - uses `padding = 100; x = Phaser.Math.Between(padding, this.sceneWidth - padding)` (100px from edges)
2. **Random y in top 40%** - uses `y = Phaser.Math.Between(50, Math.floor(0.4 * this.sceneHeight))` 
3. **showLabel() per enemy** - `if (level >= 15) { enemy.showLabel(); }` correctly placed after enemy creation

One discrepancy worth noting: the test at `battle-scene.test.js:123` expects `if (level < 15)` but the implementation uses `if (level <= 14)` — logically equivalent but a regex mismatch. The tests for x/y ranges at level >= 15 (lines 142-148) are also weak — they pass because the patterns match the level < 15 branch code, not the actual level >= 15 branch. However, the test at line 162-163 (`if (level >= 15)`) and 166-167 (`enemy.showLabel()`) do verify the new behavior correctly.

The implementation correctly satisfies the task spec with appropriate files changed (src and dist).

APPROVED