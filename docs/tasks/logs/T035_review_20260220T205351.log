=== stdout ===
The implementation is correct:

- Formula: `Math.max(800, 2500 - (level - 10) * 200)` at `battle.js:823` matches the spec exactly
- Level 10: `Math.max(800, 2500)` = 2500ms ✓
- Level 15: `Math.max(800, 1500)` = 1500ms ✓
- Level 19+: `Math.max(800, 700)` = 800ms floor ✓
- Used correctly in `startBulletFire` at `battle.js:490`, replacing the hardcoded `2000`

One note: the test-pass commit logged "No tests found, exiting with code 0" — the tests passed vacuously with no unit tests written for `getFireInterval`. The implementation itself is correct, but there's no test coverage for the boundary conditions.

APPROVED