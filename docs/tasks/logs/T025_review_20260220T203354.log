=== stdout ===
APPROVED

The bullet loop in `BattleScene.update()` is correctly implemented at `src/battle.js:305-313` (mirrored in `dist/battle.js`):

- Iterates `this.bullets` in reverse (safe for splicing)
- Calls `bullet.update(time, delta)` on each bullet
- Checks `bullet.y > this.sceneHeight` (canvas height)
- Calls `this.bridge.onBulletHit()` for the hit event
- Calls `bullet.destroy()` then `this.bullets.splice(i, 1)` to clean up

Both `src/battle.js` and `dist/battle.js` are present and consistent. Tests pass.