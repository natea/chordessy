=== stdout ===
Task T007 implemented successfully:

**Changes to `/Users/backlit/Documents/code/chordessy/neural-arpeggiator/dist/battle.js`:**

1. **Added `remapKeyPositions()` to `BattleBridge` class** (lines 113-118):
   - Accepts width and height parameters
   - Updates `battleScene.sceneWidth` and `sceneHeight`

2. **Created `handleResize()` function** (lines 147-157):
   - Recalculates container size using `getBoundingClientRect()`
   - Calls `game.scale.resize(width, height)` 
   - Notifies bridge: `battleBridge.remapKeyPositions(width, height)`

3. **Registered resize event listener** (line 179):
   - `window.addEventListener('resize', handleResize)` in `init()`