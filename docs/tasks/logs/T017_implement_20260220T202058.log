=== stdout ===
I'll help you implement task T017. Let me first understand the codebase structure and find the relevant files.
Implementation complete. The `spawnEnemies(midiNotes, level)` method in `BattleScene` now:

- Clears existing enemies before spawning new ones
- Creates Enemy instances at key-aligned positions (level < 15) or random positions with labels (level >= 15)
- Staggers spawn animations by ~100ms per enemy using `time.delayedCall`