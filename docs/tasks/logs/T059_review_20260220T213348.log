=== stdout ===
All four requirements are verified in the code:

1. **Game created once** - `startBattleGame()` guards with `if (game) return` (line 1396), wired to `DOMContentLoaded` ✓
2. **Scene paused until game starts** - `create()` ends with `this.scene.pause()` (line 315) ✓
3. **`startBattle()` resumes + resets state** - creates fresh `battleState` object (full reset, not partial), then calls `this.scene.resume()` (lines 957-995) ✓
4. **`onGameOver()` pauses + shows overlay** - clears enemies/bullets/lasers, calls `this.scene.pause()`, shows game-over overlay (lines 318-375) ✓
5. **Play Again returns cleanly** - hides overlay, shows start screen, resumes scene; subsequent tier button click calls `startBattle()` for full state reset before next game ✓

The `startBattle()` state reset is comprehensive — it constructs a new `battleState` object from scratch rather than patching fields, preventing any leaks from prior runs.

APPROVED