=== stdout ===
I'll help you implement the laser glow enhancement by adding a fourth wider semi-transparent line layer. Let me first explore the codebase to find where the laser lines are implemented.
Done! I've added a fourth wider semi-transparent glow line layer beneath the existing three laser lines. The changes include:

**New glow layer:**
- Width: 12px (widest layer, beneath outer/middle/inner)
- Color: 0x00ffff (cyan, matching other layers)
- Alpha: 0.1 (most transparent, creates outer glow effect)

**Updated in all laser lifecycle functions:**
- `onNoteOn`: Creates glow layer with other three layers
- Pulse animation: Updates glow lineStyle on yoyo/repeat
- `onChordComplete`: Enhances glow alpha to 0.2
- `clearLaser`, `onGameOver`, `onBulletHit`: Properly destroys glow layer

The glow layer renders first (beneath), followed by outer (6px), middle (3px), and inner (1px) layers, creating a stronger, more realistic laser glow effect.