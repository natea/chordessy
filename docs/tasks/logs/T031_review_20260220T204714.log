=== stdout ===
The implementation is incomplete relative to the task description. Here's what's missing:

**Task required:**
1. Decrement HP — ✅ (`this.battleState.hp--`)
2. Reset combo to 0 — **MISSING** (no `this.battleState.combo = 0`)
3. Update HP bar and combo display — **PARTIAL** (`renderLives()` is called but `updateHUD()` is not, so combo display isn't updated after reset)
4. If HP <= 0 call `onGameOver()` — ✅ (`this.bridge.onGameOver()`)
5. At levels 1-9: clear wave and spawn new chord — **MISSING** (no wave clear or chord spawn in the else branch)
6. At levels 10+: keep going — **PARTIAL** (the `if (this.waveActive) { this.waveActive = true; }` block is a no-op — it assigns true to something already expected to be true and does nothing meaningful)

There are also **no T031-specific tests** — the only `onBulletHit` tests are in `battle-bridge.test.js` and they test `BattleBridge.onBulletHit()` (which emits a `bulletHit` event), not `BattleScene.onBulletHit()`.

**REJECTED** — The `BattleScene.onBulletHit()` implementation is missing: combo reset, combo display update, and the level-conditional wave-clear/chord-spawn logic. No tests were added for T031's behavior.