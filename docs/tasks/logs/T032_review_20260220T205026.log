=== stdout ===
APPROVED

All task requirements are satisfied:

- `running=false` — `this.battleState.running = false` set immediately (line 315)
- **Clear enemies** — iterates, destroys alive ones, resets array (lines 317-322)
- **Clear bullets** — reverse-iterates, destroys each, resets array (lines 324-327)
- **Clear lasers** — destroys outer/middle/inner for each laser in the Map, calls `laserGroup.clear()` (lines 329-334)
- **Emit gameOver event** — `BattleBridge.onGameOver()` emits `'gameOver'`; `BattleScene` is wired to listen and invoke its own `onGameOver()` (lines 125-127 and 310)
- **Game over overlay with final stats** — removes `hidden` class from `#game-over-overlay`, populates score, max combo, and level (lines 336-354)
- `src/battle.js` and `dist/battle.js` are in sync