=== stdout ===
The implementation is present and correct in both `src/battle.js` and `dist/battle.js`. Verifying each requirement:

**`noteOn(midiNote)`** (line 46-60): Dedup guard, adds to heldNotes, gets x from keyXMap, emits `noteOn` with `{midiNote, x, isCorrect}`, calls `checkChord()`. ✅

**`noteOff(midiNote)`** (line 62-68): Guard for non-held note, removes from heldNotes, emits `noteOff`. ✅

**`setTargetChord(chordSymbol, midiNotes)`** (line 70-75): Stores symbol and notes (with null guard via `|| []`), emits `newTarget`. ✅

**`checkChord()`** (line 77-93): Returns early for empty target or empty heldNotes, calls `window.Chordessy.pitchClassesMatch()`, emits `chordComplete` on match, emits `wrongNote` per incorrect note (filtered by pitch class) otherwise. ✅

`pitchClassesMatch` is properly defined and exported in `shared.js:59-64`.

src and dist files are in sync.

APPROVED