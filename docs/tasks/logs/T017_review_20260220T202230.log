=== stdout ===
APPROVED

The implementation correctly satisfies all task requirements:

- **Clears existing enemies**: iterates `this.enemies`, destroys alive ones, resets to `[]`
- **level < 15**: positions each enemy at key-aligned x from `keyXMap` (with random fallback), random y, carries `isAccidental` from key data
- **level >= 15**: fully random x/y positions, calls `enemy.showLabel()`
- **Staggered spawn animations**: `this.time.delayedCall(index * 100, ...)` with alive-check guard before calling `spawnAnimation()`
- `src/battle.js` and `dist/battle.js` are in sync
- Tests in `tests/battle-scene.test.js` cover all branches and have passed