=== stdout ===
The production implementation in `src/battle.js` covers all four requirements:

1. **Bullet speed**: `getBulletSpeed(level)` = `Math.min(200, 48 + (level-1) * 8)` — starts at 48, caps at 200 ✓
2. **Fire mode**: `startBulletFire` branches on `level <= 9` (single) vs `level >= 10` (continuous, all enemies fire staggered) ✓
3. **Fire interval**: `getFireInterval(level)` = `Math.floor(2500 - (level-1) * (1800/19))` — 2500ms at L1, 700ms at L20, floored ✓
4. **Enemy position**: `spawnEnemies` uses key-aligned for `level <= 14`, random for `level >= 15` + calls `enemy.showLabel()` at 15+ ✓

The tests in `tests/difficulty-scaling.test.js` cover all the behavioral contracts (bullet speed range/cap, monotonicity, fire mode transition at L10, position mode transition at L14→L15, label visibility). The `dist/battle.js` is in sync with `src/battle.js`.

APPROVED